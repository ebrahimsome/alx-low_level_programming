#include "main.h"
/**
 * positive_or_negative - this checks if the number is negative
 */
void positive_or_negative(int i)
{
	if (i > 0)
	{
		printf("%d the number is positive\n", i);
	}
	else
	{
		printf("%d the number is negative\n", i);
	}
}
